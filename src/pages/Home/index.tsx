import Feed from "../../components/Feed";
import styles from "./styles.module.css";

const Home = () => {
  return (
    <section className="container box">
      <Feed />
    </section>
  );
};

export default Home;
